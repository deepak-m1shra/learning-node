<!DOCTYPE html>
<html lang="en">
<%- include('./partials/head.ejs') %>
<body>
    <%- include('./partials/nav.ejs') %>
    <h1>Hello, there <%= username%>(EJS)</h1>
    <div class="all-blogs">
        <h2><%= title %></h2>
        <% if(blogs.length > 0) { %>
        <% blogs.forEach(blog => { %>
            <a href="/blogs/<%=blog._id %>">
                 <h3 class="title"><%= blog.title %></h3>
                <p class="snippet"><%= blog.snippet %></p>
             <% })%>
            </a>
        <% } else {%>
        <h4>Currently, there are no blog entries </h4>
        <% }%>
</div>
    <%- include('./partials/footer.ejs') %>
</body>
</html>